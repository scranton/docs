title: User Type Abilities
description: Permissions for abilities pertaining to user types.
type: object
properties:
  create:
    description: Ability to create user types
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read user types
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update user types
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete user types
    type: string
    enum: [not_allowed, self, self_and_all_children]
