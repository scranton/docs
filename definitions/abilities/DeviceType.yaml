title: Device Type Abilities
description: Permissions for abilities pertaining to device types.
type: object
properties:
  create:
    description: Ability to create device types
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read device types
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update device types
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete device types
    type: string
    enum: [not_allowed, self, self_and_all_children]

