title: Translator Abilities
description: Permissions for abilities pertaining to translators.
type: object
properties:
  create:
    description: Ability to create translators
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read translators
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update translators
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete translators
    type: string
    enum: [not_allowed, self, self_and_all_children]

