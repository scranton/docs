title: Rule Abilities
description: Permissions for abilities pertaining to rules.
type: object
properties:
  create:
    description: Ability to create rules
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read rules
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update rules
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete rules
    type: string
    enum: [not_allowed, self, self_and_all_children]
