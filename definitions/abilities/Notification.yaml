title: Notification Abilities
description: Permissions for abilities pertaining to notifications.
type: object
properties:
  create:
    description: Ability to create notifications
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read notifications
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update notifications
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete notifications
    type: string
    enum: [not_allowed, self, self_and_all_children]

