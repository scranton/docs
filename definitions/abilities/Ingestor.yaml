title: Ingestor Abilities
description: Permissions for abilities pertaining to ingestors.
type: object
properties:
  create:
    description: Ability to create ingestors
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read ingestors
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update ingestors
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete ingestors
    type: string
    enum: [not_allowed, self, self_and_all_children]

