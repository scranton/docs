title: Integration Abilities
description: Permissions for abilities pertaining to integrations.
type: object
properties:
  create:
    description: Ability to create integrations
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read integrations
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update integrations
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete integrations
    type: string
    enum: [not_allowed, self, self_and_all_children]

