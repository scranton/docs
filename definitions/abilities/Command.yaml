title: Command Abilities
description: Permissions for abilities pertaining to commands.
type: object
properties:
  create:
    description: Ability to create commands
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read commands
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update commands
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete commands
    type: string
    enum: [not_allowed, self, self_and_all_children]

