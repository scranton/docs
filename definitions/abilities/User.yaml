title: User Abilities
description: Permissions for abilities pertaining to users.
type: object
properties:
  create:
    description: Ability to create users
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read users
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update users
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete users
    type: string
    enum: [not_allowed, self, self_and_all_children]
  act_as:
    description: Ability to act as another user in the system
    type: string
    enum: [not_allowed, self, self_and_all_children]
