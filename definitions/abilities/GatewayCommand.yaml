title: Gateway Command Abilities
description: Permissions for abilities pertaining to gateway commands.
type: object
properties:
  create:
    description: Ability to create gateway commands
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read gateway commands
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update gateway commands
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete gateway commands
    type: string
    enum: [not_allowed, self, self_and_all_children]

