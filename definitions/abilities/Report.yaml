title: Report Abilities
description: Permissions for abilities pertaining to reports.
type: object
properties:
  create:
    description: Ability to create reports
    type: string
    enum: [not_allowed, self, self_and_all_children]
  read:
    description: Ability to read reports
    type: string
    enum: [not_allowed, self, self_and_all_children]
  update:
    description: Ability to update reports
    type: string
    enum: [not_allowed, self, self_and_all_children]
  delete:
    description: Ability to delete reports
    type: string
    enum: [not_allowed, self, self_and_all_children]
